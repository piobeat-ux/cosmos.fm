{
  "name": "backend",
  "version": "1.0.0",
  "description": "Cosmos FM API with Yandex Database",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^2.1.1",
    "ydb-sdk": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
